{{define "rawtx"}}
<!DOCTYPE html>
<html lang="en">
    {{template "html-head" printf "Decode Raw Decred Transaction"}}
        {{template "navbar" . }}
        <div class="container main" data-controller="rawtx">
            <h4 class="mb-2">Decred transaction to decode or broadcast</h4>
            <form>
                <textarea
                    autofocus
                    rows="6"
                    class="col"
                    data-rawtx-target="rawTransaction"
                    data-action="keypress->rawtx#send"
                    data-event-id="decodetx"
                    placeholder="Enter the full transaction (hexadecimal encoded) here"
                ></textarea>
                <button
                    type="button"
                    data-rawtx-target="decode"
                    data-action="click->rawtx#send"
                    data-event-id="decodetx"
                    class="button btn btn-primary mr-1"
                >Decode</button>
                <button
                    type="button"
                    data-rawtx-target="broadcast"
                    data-action="click->rawtx#send"
                    data-event-id="sendtx"
                    class="button btn btn-success"
                >Broadcast</button>
            </form>
            <h4 class="mb-2" data-rawtx-target="decodeHeader">Decoded transaction</h4>
            <pre
                data-rawtx-target="decodedTransaction"
                class="json-block mono pt-3 pr-3 pb-3 pl-3"
            >
            </pre>
        </div>
        {{ template "footer" . }}
    </body>
</html>
{{end}}
